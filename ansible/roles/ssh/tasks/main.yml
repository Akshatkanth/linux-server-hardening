- name: Ensure .ssh directory exists
  file:
    path: /home/devadmin/.ssh
    state: directory
    owner: devadmin
    group: devadmin
    mode: '0700'

- name: Add SSH public key
  authorized_key:
    user: devadmin
    key: "{{ lookup('file', '~/.ssh/id_ed25519.pub') }}"